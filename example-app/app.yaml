apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: example-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/SheldonTsen/local-kind-setup
    targetRevision: fixes  # revert to main
    path: example-app/open-source
  destination:
    server: https://kubernetes.default.svc
    namespace: guestbook
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true

# kubectl patch application example-app -p '{"metadata": {"finalizers": null}}' --type merge -n argocd

