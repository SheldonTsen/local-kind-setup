apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: hello-world-template  # fixed name for reuse
  namespace: argo-workflows
spec:
  serviceAccountName: argo-workflows-sa
  entrypoint: whalesay
  templates:
    - name: whalesay
      container:
        image: rancher/cowsay
        command: [ "cowsay" ]
        args: [ "hello world" ]
        resources:
          limits: 
            memory: 32Mi
            cpu: 100m
